package ch.elca.rovl.dsl.resource.function;

/**
 * Supported authorization types.
 * NOTE only public supported, other auth is left as middleware implementation
 */
public enum AuthorizationType {
    PUBLIC
}
